import React from 'react';

class Game extends React.Component {
    render(){
        // Render JSX
        return (
          <div>
            <h1>Rendered new component!!</h1>
          </div>
        );
    }
}

export default Game;